{
  "name": "aidream-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs --port 5173",
    "postbuild": "npm run rss && echo 'Build finished in docs/.vitepress/dist'",
    "rss": "node scripts/generate-rss.mjs"
  },
  "dependencies": {
    "vitepress": "1.3.4"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
